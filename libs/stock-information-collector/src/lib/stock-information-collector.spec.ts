import { stockInformationCollector } from './stock-information-collector';

describe('stockInformationCollector', () => {
  it('should work', () => {
    expect(stockInformationCollector()).toEqual('stock-information-collector');
  });
});
